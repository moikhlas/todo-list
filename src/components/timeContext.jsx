import { createContext } from "react";

// TimerContext to manage time-related state across components
// This context can be used to share timer settings or states globally
const TimerContext = createContext();

export default TimerContext